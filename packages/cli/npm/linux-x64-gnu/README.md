# `@unstave/cli-linux-x64-gnu`

This is the **x86_64-unknown-linux-gnu** binary for `@unstave/cli`. It is installed
automatically as an optional dependency; use the `unstave` command from
`@unstave/cli`.
