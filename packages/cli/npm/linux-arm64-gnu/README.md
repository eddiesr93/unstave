# `@unstave/cli-linux-arm64-gnu`

This is the **aarch64-unknown-linux-gnu** binary for `@unstave/cli`. It is installed
automatically as an optional dependency; use the `unstave` command from
`@unstave/cli`.
