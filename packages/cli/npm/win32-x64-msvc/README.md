# `@unstave/cli-win32-x64-msvc`

This is the **x86_64-pc-windows-msvc** binary for `@unstave/cli`. It is installed
automatically as an optional dependency; use the `unstave` command from
`@unstave/cli`.
